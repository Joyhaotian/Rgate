# Configuration scope

`repro.example.json` is a relative-path template for exercising the published
scripts. It does not contain the original locked operating points, dataset
locations, cached expert-prediction identities or full inputs used for the
dissertation results.

Create a local copy outside version control before supplying those resources.
The published repository is a research-code companion rather than a
self-contained end-to-end reproducibility package.
